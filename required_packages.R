require("car") # both Methods tested requires F-testing. This is done with Anova as implemented in car package
require("tolerance") # to calculate the Tolerance Limits of Equivalent Scores, tolerance package is used
require("effects") # this package is to plot partial effects (used in DEMO scripts)
require("xtable") # this package is to create Latex tables (used only for some outputs that were then copied in Latex)
